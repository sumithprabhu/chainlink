{
  "name": "@chainlink/confidential-execution-engine-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:foundry": "forge test",
    "clean": "hardhat clean && forge clean",
    "deploy:ethereum": "hardhat run scripts/deploy.ts --network ethereum",
    "deploy:sepolia": "hardhat run scripts/deploy-sepolia.ts --network sepolia",
    "create:sepolia": "hardhat run scripts/createWorkflow-sepolia.ts --network sepolia",
    "deploy:polygon": "hardhat run scripts/deploy.ts --network polygon",
    "deploy:arbitrum": "hardhat run scripts/deploy.ts --network arbitrum",
    "deploy:base": "hardhat run scripts/deploy.ts --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
