# ==========================================================================
# CRE WORKFLOW SETTINGS FILE — confidential-score
# ==========================================================================

staging-settings:
  user-workflow:
    workflow-name: "confidential-score-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "./secrets.yaml"

# ==========================================================================
production-settings:
  user-workflow:
    workflow-name: "confidential-score-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "./secrets.yaml"
